document.addEventListener('DOMContentLoaded', async () => {
    console.log("jdjdjed")
})

const buttonloging = document.getElementById('button-login')

buttonloging.addEventListener('click', (e) => {
    e.preventDefault()
    console.log('submitted')
})